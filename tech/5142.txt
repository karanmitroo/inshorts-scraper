A 4-minute video suggests machines can read our emotions if they are converted into  numbers. Machine learning algorithms such as neural networks consist of artificial nodes that mimic our biological neurons by forming connections and exchanging information. To train the network, sample inputs pre-classified into different categories, such as photos marked happy or sad, are fed into the system.